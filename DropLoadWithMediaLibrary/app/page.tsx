'use client'

import { useState } from 'react'
import { ArrowDownToLine, Check, Clock3, Download, FileAudio, FileVideo, Link2, ListMusic, LogIn, Music2, Play, Plus, ShieldCheck, Sparkles, TerminalSquare, Video, X } from 'lucide-react'
import { ZwPlayer } from 'zwplayer-react'
import { authClient } from '@/lib/auth-client'
import { createTask } from './actions/tasks'

type Task = { id: number; url: string; formats: string; status: string; createdAt: Date; logs: string[] }
type Media = { id: number; title: string; source: string; type: 'video' | 'audio'; format: string; duration: string; url: string }

const demoTasks: Task[] = [
  { id: 1, url: 'https://youtube.com/watch?v=design-talk', formats: '视频 MP4', status: 'completed', createdAt: new Date(), logs: ['[debug] Extracting URL: https://youtube.com/watch?v=design-talk', '[youtube] design-talk: Downloading webpage', '[info] Destination: design-talk.mp4', '[download] 100% of 24.18MiB in 00:18'] },
  { id: 2, url: 'https://vimeo.com/creative-session', formats: '音频 MP3', status: 'downloading', createdAt: new Date(), logs: ['[debug] Extracting URL: https://vimeo.com/creative-session', '[vimeo] Downloading config JSON', '[info] Requested format: bestaudio', '[download] 42.7% of 8.04MiB at 1.24MiB/s ETA 00:04'] },
]

const media: Media[] = [
  { id: 1, title: '设计访谈：创造更好的产品', source: 'YouTube · design-talk', type: 'video', format: 'MP4 · 1080p', duration: '18:24', url: 'https://cdn.zwplayer.cn/media/VMAP9lxJvRpgn5sP3lV6rQ9qkzQmh5psggso3185.mp4' },
  { id: 2, title: 'Creative Session — Ambient Mix', source: 'Vimeo · creative-session', type: 'audio', format: 'MP3 · 320kbps', duration: '42:08', url: 'https://cdn.zwplayer.cn/media/VMAP9lxJvRpgn5sP3lV6rQ9qkzQmh5psggso3185.mp4' },
  { id: 3, title: '产品设计工作流分享', source: 'YouTube · workflow', type: 'video', format: 'MP4 · 720p', duration: '12:36', url: 'https://cdn.zwplayer.cn/media/VMAP9lxJvRpgn5sP3lV6rQ9qkzQmh5psggso3185.mp4' },
]

export default function Page() {
  const [mode, setMode] = useState<'download' | 'library'>('download')
  const [url, setUrl] = useState('')
  const [video, setVideo] = useState(true)
  const [audio, setAudio] = useState(false)
  const [tasks, setTasks] = useState<Task[]>(demoTasks)
  const [selectedTask, setSelectedTask] = useState<Task | null>(null)
  const [libraryTab, setLibraryTab] = useState<'video' | 'audio'>('video')
  const [selectedMedia, setSelectedMedia] = useState(media[0])
  const [authOpen, setAuthOpen] = useState(false)
  const [isSignUp, setIsSignUp] = useState(false)
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [name, setName] = useState('')
  const [error, setError] = useState('')
  const [submitting, setSubmitting] = useState(false)

  async function submitTask() {
    if (!url.trim() || (!video && !audio)) return
    const formats = [video && '视频 MP4', audio && '音频 MP3'].filter(Boolean) as string[]
    const newTask: Task = { id: Date.now(), url: url.trim(), formats: formats.join(' / '), status: 'queued', createdAt: new Date(), logs: ['[debug] Task created', `[debug] URL: ${url.trim()}`, `[debug] Requested formats: ${formats.join(', ')}`, '[info] Waiting for downloader service...'] }
    setTasks((current) => [newTask, ...current]); setUrl('')
    try { await createTask(newTask.url, formats) } catch { /* 未登录时保留前端演示 */ }
  }

  async function submitAuth() {
    setSubmitting(true); setError('')
    const result = isSignUp ? await authClient.signUp.email({ email, password, name }) : await authClient.signIn.email({ email, password })
    if (result.error) setError('邮箱或密码不正确，请检查后重试。'); else { setAuthOpen(false); setEmail(''); setPassword(''); setName('') }
    setSubmitting(false)
  }

  const libraryItems = media.filter((item) => item.type === libraryTab)

  return (
    <main className="min-h-screen bg-[#f4f7fb] text-[#172033]">
      <header className="border-b border-[#e3e9f2] bg-white"><div className="mx-auto flex h-[58px] max-w-[1320px] items-center justify-between px-3.5 sm:h-[68px] sm:px-5 lg:px-8"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#1769e0] text-white shadow-sm"><ArrowDownToLine size={21} /></div><span className="text-[19px] font-bold tracking-tight">Drop<span className="text-[#1769e0]">Load</span></span></div><div className="flex items-center gap-2"><nav className="hidden rounded-lg bg-[#f1f5fa] p-1 sm:flex"><button onClick={() => setMode('download')} className={`rounded-md px-3 py-1.5 text-sm font-semibold ${mode === 'download' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}>下载器</button><button onClick={() => setMode('library')} className={`rounded-md px-3 py-1.5 text-sm font-semibold ${mode === 'library' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}>媒体库</button></nav><button onClick={() => setAuthOpen(true)} className="flex items-center gap-2 rounded-lg border border-[#dce4ef] px-3 py-2 text-sm font-medium text-[#445269] transition hover:border-[#1769e0] hover:text-[#1769e0]"><LogIn size={16} /> 登录 / 注册</button></div></div></header>
      <div className="mx-auto max-w-[1320px] px-3 py-3 sm:px-5 sm:py-6 lg:px-8">
        <div className="mb-3 flex rounded-lg bg-[#eaf1fb] p-1 sm:hidden"><button onClick={() => setMode('download')} className={`flex-1 rounded-md py-2 text-sm font-semibold ${mode === 'download' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}>下载器</button><button onClick={() => setMode('library')} className={`flex-1 rounded-md py-2 text-sm font-semibold ${mode === 'library' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}>媒体库</button></div>
        {mode === 'download' ? <div className="grid gap-3 lg:grid-cols-[minmax(0,1fr)_390px] lg:gap-6"><section className="rounded-2xl border border-[#e2e8f0] bg-white p-4 shadow-[0_8px_30px_rgba(42,72,112,0.05)] sm:min-h-[620px] sm:p-6 lg:p-10"><div className="flex flex-col justify-center sm:min-h-[540px]"><div className="mb-4 hidden w-fit items-center gap-2 rounded-full bg-[#e8f1ff] px-3 py-1.5 text-xs font-semibold text-[#1769e0] sm:flex"><Sparkles size={14} /> 简单、快速、无广告</div><h1 className="max-w-[620px] text-[30px] font-bold tracking-[-0.04em] text-[#14213d] sm:text-4xl lg:text-5xl">把链接变成文件</h1><p className="mt-2 max-w-[600px] text-sm leading-6 text-[#718096] sm:mt-4 sm:text-[16px] sm:leading-7">粘贴来自 YouTube、Vimeo 或其他网站的视频链接，选择需要的格式，任务会自动加入右侧列表。</p><div className="mt-6 max-w-[760px] rounded-2xl border border-[#e1e8f1] bg-[#fbfcfe] p-2.5 shadow-[0_12px_40px_rgba(40,73,120,0.08)] sm:mt-9 sm:p-3"><div className="flex flex-col gap-2 rounded-xl border border-[#dce4ef] bg-white p-2 focus-within:border-[#1769e0] focus-within:ring-4 focus-within:ring-[#1769e0]/10 sm:flex-row sm:items-center sm:px-4 sm:py-1.5"><Link2 size={19} className="hidden shrink-0 text-[#91a0b5] sm:block" /><input value={url} onChange={(e) => setUrl(e.target.value)} placeholder="粘贴视频或音频链接..." className="min-w-0 flex-1 bg-transparent px-2 py-2.5 text-[15px] outline-none placeholder:text-[#9aa8ba] sm:px-0 sm:py-3" /><button onClick={submitTask} disabled={!url.trim() || (!video && !audio)} className="flex w-full items-center justify-center gap-2 rounded-lg bg-[#1769e0] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#0f58c4] disabled:cursor-not-allowed disabled:opacity-40 sm:w-auto"><Plus size={16} /> 加入队列</button></div><div className="mt-3 flex flex-nowrap items-center gap-x-3 overflow-x-auto px-2 text-sm text-[#65748a] sm:gap-5"><span className="mr-1 shrink-0 text-xs font-semibold tracking-wider text-[#9aa8ba]">下载格式</span><label className="flex shrink-0 cursor-pointer items-center gap-1.5 whitespace-nowrap"><input type="checkbox" checked={video} onChange={(e) => setVideo(e.target.checked)} className="peer sr-only" /><span className="flex h-4 w-4 items-center justify-center rounded border border-[#b8c5d6] text-transparent peer-checked:border-[#1769e0] peer-checked:bg-[#1769e0] peer-checked:text-white"><Check size={12} /></span><FileVideo size={16} /> 视频 MP4</label><label className="flex shrink-0 cursor-pointer items-center gap-1.5 whitespace-nowrap"><input type="checkbox" checked={audio} onChange={(e) => setAudio(e.target.checked)} className="peer sr-only" /><span className="flex h-4 w-4 items-center justify-center rounded border border-[#b8c5d6] text-transparent peer-checked:border-[#1769e0] peer-checked:bg-[#1769e0] peer-checked:text-white"><Check size={12} /></span><FileAudio size={16} /> 音频 MP3</label></div></div><div className="mt-6 flex items-center gap-2 text-xs text-[#9aa8ba]"><ShieldCheck size={14} /> 链接仅用于处理任务，我们尊重你的隐私</div></div></section><aside className="flex max-h-[420px] flex-col overflow-hidden rounded-2xl border border-[#e2e8f0] bg-white shadow-[0_8px_30px_rgba(42,72,112,0.05)] sm:min-h-[620px] sm:max-h-none"><div className="border-b border-[#edf1f5] px-5 py-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">任务列表</h2><p className="mt-1 text-sm text-[#8794a7]">下载任务会显示在这里</p></div><span className="rounded-full bg-[#e8f1ff] px-3 py-1 text-xs font-semibold text-[#1769e0]">{tasks.length} 个任务</span></div></div><div className="flex-1 overflow-y-auto">{tasks.map((task) => <div role="button" tabIndex={0} key={task.id} onClick={() => setSelectedTask(task)} onKeyDown={(event) => { if (event.key === 'Enter' || event.key === ' ') setSelectedTask(task) }} className="flex w-full cursor-pointer items-start gap-3 border-b border-[#edf1f5] px-5 py-4 text-left transition hover:bg-[#f8fbff] last:border-0"><div className={`mt-0.5 flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${task.status === 'completed' ? 'bg-[#e9f8ef] text-[#24a05a]' : task.status === 'downloading' ? 'bg-[#fff6df] text-[#d18a00]' : 'bg-[#edf3ff] text-[#1769e0]'}`}>{task.status === 'completed' ? <Check size={17} /> : task.status === 'downloading' ? <Clock3 size={17} /> : <Download size={17} />}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-[#334155]">{task.url}</p><div className="mt-1 flex flex-wrap items-center gap-1.5 text-xs text-[#97a3b3]"><span>{task.formats}</span><span>·</span><span>{task.status === 'completed' ? '已完成' : task.status === 'downloading' ? '处理中...' : '等待处理'}</span></div>{task.status === 'completed' && <span className="mt-2 flex items-center gap-1.5 text-xs font-semibold text-[#1769e0]"><Download size={13} /> 下载文件</span>}</div></div>)}</div></aside></div> : <section className="grid gap-3 lg:grid-cols-[minmax(0,1fr)_390px] lg:gap-6"><div className="rounded-2xl border border-[#e2e8f0] bg-white p-3 shadow-[0_8px_30px_rgba(42,72,112,0.05)] sm:p-5"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#1769e0]">媒体库</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-[#14213d] sm:text-3xl">正在播放</h1></div><span className="hidden items-center gap-2 rounded-full bg-[#eef6ff] px-3 py-1.5 text-xs font-medium text-[#1769e0] sm:flex"><ListMusic size={14} /> {libraryItems.length} 个媒体</span></div><div className="aspect-video overflow-hidden rounded-xl bg-[#0b1220] shadow-inner"><ZwPlayer key={selectedMedia.id} murl={selectedMedia.url} fluid autoplay={false} disableMutedConfirm optionButton infoButton className="h-full w-full" /></div><div className="mt-4 rounded-xl bg-[#f6f9fd] p-4"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#e6efff] text-[#1769e0]">{selectedMedia.type === 'video' ? <Video size={19} /> : <Music2 size={19} />}</div><div className="min-w-0"><h2 className="truncate text-base font-bold text-[#1e2b42]">{selectedMedia.title}</h2><p className="mt-1 text-sm text-[#8491a3]">{selectedMedia.source} · {selectedMedia.format} · {selectedMedia.duration}</p></div></div></div></div><aside className="flex min-h-[460px] flex-col overflow-hidden rounded-2xl border border-[#e2e8f0] bg-white shadow-[0_8px_30px_rgba(42,72,112,0.05)] lg:min-h-[620px]"><div className="border-b border-[#edf1f5] px-5 py-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">播放列表</h2><p className="mt-1 text-sm text-[#8794a7]">选择已下载的媒体</p></div><ListMusic size={20} className="text-[#9aa8ba]" /></div><div className="mt-4 flex rounded-lg bg-[#f1f5fa] p-1"><button onClick={() => setLibraryTab('video')} className={`flex flex-1 items-center justify-center gap-1.5 rounded-md py-2 text-sm font-semibold ${libraryTab === 'video' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}><Video size={15} /> 视频</button><button onClick={() => setLibraryTab('audio')} className={`flex flex-1 items-center justify-center gap-1.5 rounded-md py-2 text-sm font-semibold ${libraryTab === 'audio' ? 'bg-white text-[#1769e0] shadow-sm' : 'text-[#718096]'}`}><Music2 size={15} /> 音频</button></div></div><div className="flex-1 overflow-y-auto p-2">{libraryItems.map((item) => <button key={item.id} onClick={() => setSelectedMedia(item)} className={`flex w-full items-start gap-3 rounded-xl p-3 text-left transition ${selectedMedia.id === item.id ? 'bg-[#eef5ff]' : 'hover:bg-[#f7f9fc]'}`}><div className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-lg ${item.type === 'video' ? 'bg-[#e7efff] text-[#1769e0]' : 'bg-[#f0ebff] text-[#7855c9]'}`}>{item.type === 'video' ? <Video size={18} /> : <Music2 size={18} />}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-[#334155]">{item.title}</p><p className="mt-1 text-xs text-[#97a3b3]">{item.format} · {item.duration}</p></div>{selectedMedia.id === item.id && <Play size={14} className="mt-1 shrink-0 fill-[#1769e0] text-[#1769e0]" />}</button>)}</div></aside></section>}
      </div>
      {selectedTask && <div className="fixed inset-0 z-40 bg-[#13213b]/20" onClick={() => setSelectedTask(null)}><section role="dialog" aria-label="任务详情日志" className="absolute inset-y-0 right-0 flex w-full max-w-[460px] flex-col border-l border-[#dfe7f1] bg-white shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-[#edf1f5] px-5 py-5"><div><div className="flex items-center gap-2 text-[#1769e0]"><TerminalSquare size={17} /><span className="text-xs font-semibold uppercase tracking-wider">任务详情</span></div><h2 className="mt-2 max-w-[330px] truncate text-base font-bold text-[#172033]">{selectedTask.url}</h2></div><button type="button" aria-label="关闭任务详情" onClick={() => setSelectedTask(null)} className="rounded-lg p-2 text-[#8794a7] hover:bg-[#f1f5fa]"><X size={20} /></button></div><div className="border-b border-[#edf1f5] px-5 py-4"><div className="flex justify-between text-xs text-[#8794a7]"><span>{selectedTask.formats}</span><span className="font-semibold text-[#1769e0]">{selectedTask.status === 'completed' ? '已完成' : '处理中...'}</span></div></div><div className="flex-1 overflow-y-auto bg-[#101827] p-5 font-mono text-[12px] leading-6 text-[#b9c7d9]">{selectedTask.logs.map((log, index) => <p key={`${selectedTask.id}-${index}`} className="break-words"><span className="mr-3 select-none text-[#53647b]">{String(index + 1).padStart(2, '0')}</span>{log}</p>)}</div></section></div>}
      {authOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#13213b]/30 p-4" onClick={() => setAuthOpen(false)}><div className="w-full max-w-[400px] rounded-2xl bg-white p-7 shadow-2xl" onClick={(e) => e.stopPropagation()}><div className="mb-6 flex items-start justify-between"><div><h2 className="text-xl font-bold">{isSignUp ? '创建你的账号' : '欢迎回来'}</h2><p className="mt-1 text-sm text-[#8794a7]">{isSignUp ? '保存并管理你的下载任务' : '登录后查看你的任务记录'}</p></div><button onClick={() => setAuthOpen(false)} className="text-[#95a1b2]"><X size={20} /></button></div><div className="space-y-3">{isSignUp && <input value={name} onChange={(e) => setName(e.target.value)} placeholder="你的昵称" className="w-full rounded-lg border border-[#dce4ef] px-3 py-3 text-sm outline-none focus:border-[#1769e0]" />}<input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="邮箱地址" className="w-full rounded-lg border border-[#dce4ef] px-3 py-3 text-sm outline-none focus:border-[#1769e0]" /><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="密码（至少 8 位）" className="w-full rounded-lg border border-[#dce4ef] px-3 py-3 text-sm outline-none focus:border-[#1769e0]" />{error && <p className="text-sm text-red-500">{error}</p>}<button onClick={submitAuth} disabled={submitting || !email || !password || (isSignUp && !name)} className="w-full rounded-lg bg-[#1769e0] py-3 text-sm font-semibold text-white disabled:opacity-40">{submitting ? '处理中...' : isSignUp ? '注册账号' : '登录'}</button></div><button onClick={() => { setIsSignUp(!isSignUp); setError('') }} className="mt-5 w-full text-center text-sm text-[#1769e0]">{isSignUp ? '已有账号？立即登录' : '还没有账号？注册一个'}</button></div></div>}
    </main>
  )
}
