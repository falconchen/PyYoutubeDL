; Supervisor Web 面板配置模板。
;
; 用法：复制到 /etc/supervisor/conf.d/inet-http-server.conf，按下面说明填写
; port 与 password，然后 chmod 600 并 systemctl restart supervisor。
;
; 独立成 include 文件，不要写进 /etc/supervisor/supervisord.conf ——
; 那是发行版自带文件，apt 升级时可能被覆盖。
; 也不要写进 pyyoutubedl.conf —— install.sh 每次都会重新渲染该文件。
;
; 安全要点：
;   * 面板可启停进程、读取日志，权限等同 root 操作服务，切勿暴露到公网。
;   * supervisor 只提供 HTTP Basic Auth 且不支持 TLS，密码在网络上明文传输。
;   * port 只能填一个地址，不能列多个，也不能写两个 [inet_http_server] section。
;     - 最安全：127.0.0.1:9002，再用 ssh -L 9002:127.0.0.1:9002 转发访问；
;     - 单个局域网：填本机对应网卡的 IP；
;     - 多个网段：只能用 *:9002，但会连同所有 docker 网桥一起监听，
;       等于把面板暴露给本机每个容器。
;   * 省略 username / password 即为完全无认证，任何能连到端口的人都能启停服务。
;   * 端口需先确认空闲：ss -lnt | grep 9002。9001 常被 portainer 等占用。
;
; 密码以 SHA1 存储，不要写明文。生成方式：
;   printf '%s' '你的密码' | sha1sum
; 取输出的十六进制串，填到 {SHA} 之后。

[inet_http_server]
port=127.0.0.1:9002
username=CHANGE_ME
password={SHA}CHANGE_ME_TO_SHA1_HEX
